if [ -f "package.json" ] && [ -f "angular.json" ]; then
  npm run lint-staged
elif [ -d "CleanFix-frontend" ]; then
  cd CleanFix-frontend && npm run lint-staged
elif [ -d "../CleanFix-frontend" ]; then
  cd ../CleanFix-frontend && npm run lint-staged
else
  echo "⚠️  No se encontró el proyecto Angular. Verificar estructura de directorios."
  exit 1
fi  